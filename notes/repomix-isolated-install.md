Usar o:
```
curl -fsSL https://fnm.vercel.app/install | bash
source ~/.bashrc  # ou reinicie o terminal
fnm install --lts
```

Finalmente no repositório que quiser documentar, usar:
```
npx repomix --style xml
```
